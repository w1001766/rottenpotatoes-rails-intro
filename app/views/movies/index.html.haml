%h1 All Movies
= form_tag movies_path, :method => :get, :id => 'ratings_form' do
  Include:
  - @all_ratings.each do |rating|
    = rating
    = check_box_tag "ratings[#{rating}]", 1, params[:"#{rating}"]
  = submit_tag 'Refresh', :id => 'ratings_submit'
%table#movies	%table#movies
  %thead	  %thead
    %tr	    %tr
      %th{:class => ("hilite" if params[:sort] == "title")}= link_to 'Movie Title', movies_path(:sort => 'title'), :id => 'title_header'	      %th{:class => ("hilite" if params[:sort] == "title")}= link_to 'Movie Title', movies_path(:sort => 'title'), :id => 'title_header'
      %th Rating	      %th Rating
      %th{:class => ("hilite" if params[:sort] == "release_date")}= link_to 'Release Date', movies_path(:sort => 'release_date'), :id => 'release_date_header'	      %th{:class => ("hilite" if params[:sort] == "release_date")}= link_to 'Release Date', movies_path(:sort => 'release_date'), :id => 'release_date_header'
      %th More Info	      %th More Info
  %tbody	  %tbody
    - @movies.each do |movie|	    - @movies.each do |movie|
      %tr	      %tr
        %td= movie.title 	        %td= movie.title 
        %td= movie.rating	        %td= movie.rating
        %td= movie.release_date	        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie_path(movie)	        %td= link_to "More about #{movie.title}", movie_path(movie)


 = link_to 'Add new movie', new_movie_path	= link_to 'Add new movie', new_movie_path 